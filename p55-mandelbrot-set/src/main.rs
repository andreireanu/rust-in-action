use num::complex::Complex;


fn main() {
    // let mandelbrot = calculate mandelbrot();
    // render_mandelbrot();
}

fn calculate_mandelbrot(
    max_iters: usize,
    x_min: f64,
    x_max: f64,
    y_min: f64,
    y_max: f64,
    width: usize,
    height: usize,
    ) -> Vec<Vec<usize>>;
    {
        let mut rows: Vec<_> = Vec::with_capacity(capacity)
    }



