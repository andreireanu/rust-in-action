// The basic difference between the register and memory is that 
// the REGISTER holds the data that CPU is currently processing whereas, 
// the MEMORY holds program instruction and data that the program requires for execution.

struct CPU {
    registers: [u8; 16],
}

fn main() {

}
